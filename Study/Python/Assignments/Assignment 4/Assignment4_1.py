#1.Write a program which contains one lambda function which accepts one parameter and return
#power of two.

powerof2 = lambda num : num * num;

num = int(input("Enter the number :"));
print("power of the number is ",powerof2(num));